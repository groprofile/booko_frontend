# Used automatically by `vite build` (production mode) — overrides .env.
# Vite inlines VITE_ vars into the client bundle at build time; they are public,
# not secret. Local `npm run dev` keeps using .env (localhost:5500).
VITE_API_BASE_URL=https://backend-bokko1.onrender.com/api/v1
